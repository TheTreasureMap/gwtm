Remote Access [enabled]:

If you have iptables enabled and want to connect to the MySQL database from another machine, you must open a port in your server’s firewall (the default port is 3306). You don’t need to do this if the application that uses MySQL is running on the same server.

Start Database:

> systemctl start mysql

Starts on reboot:

> systemctl enable mysql

Enter mysql:

> mysql -u root -p [pwd]

> create database gwtreasuremap

> use gwtreasuremap

> create table Users ( ID INT AUTO_INCREMENT PRIMARY KEY, username varchar(25), firstname varchar(25), lastname varchar(25), datecreated datetime);

> create table UserGroups ( userID INT, groupID INT, role varchar(25));

> create table Groups ( ID INT AUTO_INCREMENT PRIMARY KEY, name varchar(25), datecreated datetime);

> create table UserActions ( ID INT AUTO_INCREMENT PRIMARY KEY, modified_table varchar(25), modified_id INT, modified_column varchar(25), prev_value varchar(25), new_value varchar(25), type varchar(25), time datetime);

> create table Instrument (ID INT AUTO_INCREMENT PRIMARY KEY, instrument_name varchar(100), instrument_type ENUM('photometric', 'spectroscopic'), footprint geometry, datecreated datetime, submitterID INT);

> create table Pointing (ID INT AUTO_INCREMENT PRIMARY KEY, status ENUM('planned', 'completed', 'cancelled'),  position geometry, galaxy_catalog INT, galaxy_catalogID INT, instrumentID INT, depth float, time datetime,  datecreated datetime, submitterID INT);

> create table Pointing_Event (pointingID INT, GraceID text);

> create table Glade_2p3 (ID INT AUTO_INCREMENT PRIMARY KEY, position geometry, gwgc_name varchar(50), hyperleda_name varchar(50), 2mass_name varchar(50), sdssdr12_name varchar(50), distance float, distance_error float, redshift float, bmag float, bmag_err float, bmag_abs float, jmag float, jmag_err float, hmag float, hmag_err float, kmag float, kmag_err float, flag1 CHAR, flag2 INT, flag3 INT);

> create table GW_Alert (ID INT AUTO_INCREMENT PRIMARY KEY, GraceID text, role text, timesent datetime, time_of_signal datetime, packet_type INT, alert_type text, detectors varchar(50), description text, far float, skymap_fits_url text, distance float, distance_error float, prob_bns float, prob_nsbh float, prob_gap float, prob_bbh float, prob_terrestrial float, prob_hasns float, prob_hasremenant float, datecreated datetime);




##Phase 2

create table Spectroscopy (ID INT AUTO_INCREMENT PRIMARY KEY, candidateID INT, instrumentID INT, time datetime, classification INT, URL varchar(300), is_limit bool, datecreated datetime, submitterID INT)

create table Photometry (ID INT AUTO_INCREMENT PRIMARY KEY, candidateID INT, instrumentID INT, time datetime, magnitude float, magnitude_error float, flux float, flux_error float, band INT, is_limit bool, datecreated datetime, submitterID INT);

create table Candidate (ID INT AUTO_INCREMENT PRIMARY KEY, status INT,  position geometry, classificationID INT, redshift float, datecreated datetime, submitterID INT);

create table Candidate_Event (candidateID INT, GraceID text);





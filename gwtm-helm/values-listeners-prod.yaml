# Production values for listeners deployment
# This file overrides values-listeners.yaml with production-specific settings

global:
  useGeneratedSecrets: false
  existingSecret: gwtm-listener-secrets

listeners:
  image:
    tag: "1.0.0"

  # GWTM API configuration
  # Points to prod cluster ingress. No host filter on ingress so IP-based requests work.
  api:
    host: "149.165.151.27"  # Prod cluster floating IP
    port: 80
    path: "/api/v1/"

  # Storage configuration
  storage:
    type: "swift"
    swift:
      authUrl: "https://js2.jetstream-cloud.org:5000/v3"
      storageUrl: "https://js2.jetstream-cloud.org:8001/swift/v1"
      projectName: "PHY250291"
      containerName: "gwtreasuremap"
      userDomainName: "Default"
      projectDomainName: "Default"

  # Production settings
  dryRun: false
  writeToStorage: true
  verbose: false

  logging:
    format: "json"
    level: "INFO"

  observingRun: "O4"

  galaxyCatalogConfig: "/app/gal_catalog_config.ini"
